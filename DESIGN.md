# DESIGN.md — Fluxketch 사이트 v3 "제품 무대(Product Stage)"

오너 지시(2026-09-10): Shapr3D(shapr3d.com)의 구조·모션 수준을 기준으로 삼는 **정석 제품 랜딩**. 세계는 카테고리 정석을 최고 완성도로 — 밝은 스튜디오 화이트 무대와 깊은 그래파이트 무대가 번갈아 오고, 제품 화면(실도면 스크린샷)이 iPad 프레임 안에서 주인공이다.

## 무대
- 밝은 무대 `--bg: #ffffff` / 옅은 무대 `--bg-2: #f4f5f7` / 어두운 무대 `--ink: #0f1115` / 어두운 무대의 카드 `#171a20`.
- 글자: 밝은 무대 `#0f1115`, 보조 `#5b6270`; 어두운 무대 `#f4f5f7`, 보조 `#a7adb8`(회색이 아니라 무대 색에서 틴트).
- 브랜드 초록 `--brand: #2fb56a`(단추·링크·강조 한 가지). 그라데이션 글자 금지. 액센트 두 번째 색 없음.
- 도면 화면은 손대지 않는다(The Pure Canvas Rule) — 프레임·그림자만 얹는다.

## 글꼴
- Pretendard Variable 한 벌(ko·en 공용). 표제 700 / 소제 600 / 본문 400. 표제 tracking −0.03em, 본문 1.6 줄간격. 한국어 `word-break: keep-all`. 수치 `font-variant-numeric: tabular-nums`.
- 표제 크기: 히어로 clamp(40px, 6.2vw, 76px), 섹션 clamp(30px, 4vw, 52px), 소제 20–24px, 본문 17–18px.

## 구성 요소
- 상단 내비: 고정, 스크롤 시 반투명 흰 판 + 블러 + 하단 헤어라인. 오른쪽 끝 = 초록 **알약 단추**(베타 신청) + 언어 토글.
- 단추: 알약(높이 48/56), 초록 채움 + 흰 글자 / 유령(헤어라인). 호버 = 밝기 −6%·살짝 위로 1px.
- **디바이스 프레임**: iPad — 검정 베젤 3.2%·모서리 반경 3.4%·안쪽 화면 반경 2%·부드러운 그림자(오프셋 24px·블러 60px). 포트레이트/랜드스케이프 두 종.
- 기능 탭(Shapr3D식): 왼쪽 탭 목록(제목+한 줄), 오른쪽 큰 화면. 6초 자동 진행 + 진행 막대, 탭 클릭으로 즉시 전환, 호버 시 정지.
- 갤러리 스트립: 실도면 썸네일 가로 무한 이동(호버 정지). 캡션 = 도면 이름·모드.
- 통계 행: 큰 수치는 **본문 안 문장**으로만(대형 숫자 타일 금지) — 카운트업은 인라인 숫자에만.
- FAQ: 디스클로저(details/summary), 첫 항목 열림.
- 푸터: 5열(제품·기능·자료·회사·법적) + 소셜 없음(사실: 계정 없음) + 저작권.

## 모션(한 벌)
- 등장: `.reveal` 요소가 뷰포트 15% 진입 시 `opacity 0→1, translateY 18px→0, filter blur(6px)→0`, 600ms, cubic-bezier(.2,.7,.2,1). 섹션마다 동일 반복 금지 — 히어로는 순차(표제→부제→단추→기기), 갤러리는 스트립 이동, 성능 절은 숫자 카운트업, 기능 절은 탭 크로스페이드가 각 절의 "한 번의 모션".
- 히어로 기기: 마우스 위치에 따라 최대 ±3° 3D 기울기(데스크톱만), 초록 치수선이 SVG stroke로 1회 그려진다.
- `prefers-reduced-motion: reduce`면 전부 정지(스트립·카운트업·기울기·자동 탭).

## 금지
- 그라데이션 글자, 유리/글로우 장식(내비 블러만 예외 — 기능이다), 동일 카드 격자로 페이지 구성, 절마다 대문자 아이브로, 큰 숫자 타일, 두 번째 액센트 색, 모노스페이스 장식.
- 없는 사실: 3D·AI·클라우드 협업·고객사 로고·평점. 가격·베타 일정은 PRODUCT.md만 따른다.
